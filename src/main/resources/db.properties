# GAPE - Configuracao de acesso a base de dados (EXEMPLO)
# NAO colocar credenciais reais aqui. Para ambiente local, criar
# src/main/resources/db-local.properties (ignorado pelo Git).

db.url=jdbc:mysql://localhost:3306/gape?useSSL=false&serverTimezone=UTC
db.user=gape_user
db.password=CHANGE_ME
db.driver=com.mysql.cj.jdbc.Driver

# Pool / ligacao (TODO Fase 1+: ajustar)
db.pool.maxConnections=10
